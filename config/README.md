# Configuration Files

This directory contains application configuration files and settings.

## Files

- `settings.json` - Application settings
- `theme.json` - Theme configuration
- `hotkeys.json` - Hotkey mappings

(Files to be added as needed)
